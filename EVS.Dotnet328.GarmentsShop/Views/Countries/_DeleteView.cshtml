@using EVS.Dotnet328.GarmentsShop.Models
@model CountryModel

@using (Html.BeginForm("delete", "countries", FormMethod.Post, new { id = "deletecountryform" }))
{
    <div class="form-group">
        <h4><label class="bg-success">Current Name: <small>@Model.Name</small> </label></h4>
        <div class="input-group has-feedback">
            <span class="input-group-addon"><span class="glyphicon glyphicon-tags"></span></span>
            @Html.TextBoxFor(m => m.Code, new { Placeholder = "Country Code", @class = "form-control", disabled = "true" })
            <span class="form-control-feedback"></span>
        </div>
    </div>

    <div class="form-group">
        <div class="input-group has-feedback">
            <span class="input-group-addon"><span class="glyphicon glyphicon-bookmark"></span></span>
            @Html.TextBoxFor(m => m.Name, new { placeholder = "Department Name", @class = "form-control", disabled = "true" })
            <span class="form-control-feedback"></span>
        </div>
    </div>

    <div class="form-group modal-footer">
        <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-check"></span>  Yes</button>
        <button type="button" class="btn btn-danger" data-dismiss="modal"><span class="glyphicon glyphicon-remove-circle"></span>  No</button>
    </div>
}

